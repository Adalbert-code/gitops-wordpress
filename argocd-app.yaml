apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: wordpress-platform
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/adalbert-code/gitops-wordpress.git
    targetRevision: main
    path: apps
  destination:
    server: https://kubernetes.default.svc
    namespace: wordpress
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
